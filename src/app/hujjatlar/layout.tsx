import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Hujjatlar",
};

export default function HujjatlarLayout({ children }: { children: React.ReactNode }) {
  return children;
}
