import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Axborot xizmatlari",
};

export default function AxborotLayout({ children }: { children: React.ReactNode }) {
  return children;
}
