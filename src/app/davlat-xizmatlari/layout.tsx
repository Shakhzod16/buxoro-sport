import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Davlat xizmatlari",
};

export default function DavlatLayout({ children }: { children: React.ReactNode }) {
  return children;
}
