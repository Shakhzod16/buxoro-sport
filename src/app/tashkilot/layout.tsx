import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tashkilot haqida",
};

export default function TashkilotLayout({ children }: { children: React.ReactNode }) {
  return children;
}
