import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Faoliyat",
};

export default function FaoliyatLayout({ children }: { children: React.ReactNode }) {
  return children;
}
